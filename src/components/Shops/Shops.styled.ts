import styled from 'styled-components'

export const ShopsEl = styled.aside`
   position: fixed;
   top: 60px;
   left: 0;
   min-height: 100%;
   width: 400px;
   border: 1px solid var(--color-grey);
   padding: 30px;
`

export const ShopTitle = styled.h4`
   text-align: center;
   font-size: var(--fs-md);
   font-weight: var(--fw-bold);
   margin-bottom: 10px;
`

